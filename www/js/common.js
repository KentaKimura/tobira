$(function(){//スムーススクロール$(document).on('click','a[href^=#]',function(){	if( !$(this).hasClass('inline')){		var speed = 500;		var href= $(this).attr("href");		var position = $(href).offset().top -150;		$("html, body").animate({scrollTop:position}, speed, "swing");		return false; //#アンカーがつかないように	}});//メニュースクロール固定var gnav = $('.gnav'),gnav_fix = $('#gnav-fix'),gnav_fixed = false;$(window).scroll(function(){	var $this = $(this),	st = $this.scrollTop();	if(st > 890 && gnav_fixed == false){		gnav_fix.css({			top: 0		});		gnav_fixed = true;	} else if(st < 890 && gnav_fixed == true){		gnav_fix.css({			top : '-100px'		});		gnav_fixed = false;	}});/* アニメーションポイントの設定 */var i = 0, pos = [], anim_point = [], anim_block = [];var win_height = $(window).height();    // ウインドウ(ブラウザ)の高さの取得// class-animの位置を配列に$('.intro').each(function(){	anim_block.push($(this));	pos.push($(this).offset().top);})// ウインドウ高さなどで調整for(var p = 0 ; p < pos.length ; p++){	anim_point.push(pos[p] - win_height + 150);}// スクロールに合わせて実行$(window).scroll(function() {	while($(this).scrollTop() > anim_point[i]){		anim_block[i].addClass('show');		i++;	}});});//ページTOPへ$(function(){	//ボタン[id:page-top]を出現させるスクロールイベント	$(window).scroll(function(){		//最上部から現在位置までの距離を取得して、変数[now]に格納		var now = $(window).scrollTop();		//最下部から現在位置までの距離を計算して、変数[under]に格納		var under = $('body').height() - (now + $(window).height()); 		//最上部から現在位置までの距離(now)が1500以上かつ		//最下部から現在位置までの距離(under)が200px以上だったら		if(now > 900){			//[#page-top]をゆっくりフェードインする			$('#page-top').fadeIn('slow');		//それ以外だったらフェードアウトする		}else{			$('#page-top').fadeOut('slow');		}	}); 	//ボタン(id:move-page-top)のクリックイベント	$('#move-page-top').click(function(){		//ページトップへ移動する		$('html,body').animate({scrollTop:0},'slow');	});});